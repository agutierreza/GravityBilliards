feat: Implement decoupled GameLoop, SimulationRunner, and Command pattern

- Introduced `GameCommand` interface and concrete implementations (`ApplyThrustCommand`, `ApplyForceCommand`) to encapsulate agnostic mathematical inputs.
- Created `GameLoop` class to orchestrate tick-by-tick physics simulation and handle command execution deterministically.
- Defined `InputProvider` interface for abstracting UI/peripheral inputs.
- Developed `SimulationRunner` using an accumulator pattern to safely bridge real-world elapsed time (lag/variable framerates) into fixed-step GameLoop ticks, dynamically querying the `InputProvider`.
- Added comprehensive E2E validation tests (`test_game_loop.cpp` and `test_simulation_runner.cpp`) that trace and perfectly match the mathematical ground-truths of the original engine.
